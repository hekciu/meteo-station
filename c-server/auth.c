int auth(char * authHeaderContent, size_t authHeaderSize) {
    //TODO: finish this function and create top level common/ folder with base64.h header-defined function
    if (authHeaderSize <= 0) {
        return -1;
    }
}
